# 3D Simulator — 전체 프로젝트 지침

## 이게 뭔가
여러 분야의 기술 제품을 **3D 분해도(exploded view)**로 분해·탐구하며 배우는 교육용 웹사이트.
사용자는 제품을 회전하고, 스크롤·핀치·슬라이더로 부품을 펼치고(분해) 합치며(조립),
부품을 클릭하면 그 역할 설명을 본다. 카테고리와 모델은 계속 늘어난다.

브랜드명은 임시로 **STRATA** (자리표시). 확정되면 한 곳에서 바꿀 수 있게 한다.

---

## 메모리 구조 (꼭 이해할 것)
클로드 코드는 작업 중인 폴더 기준으로 **상위의 모든 `CLAUDE.md`를 읽는다.** 그래서 3단계로 둔다:
- **루트 `CLAUDE.md`(이 파일)** — 모든 곳에 공통인 규칙 (스택, 디자인, 워크플로우, 다국어, 사이트 구조).
- **카테고리 `CLAUDE.md`** (예: `semiconductor/CLAUDE.md`) — 그 분야 전반의 지침.
- **모델 `CLAUDE.md`** (예: `semiconductor/hbm/CLAUDE.md`) — 개별 모델의 구조·색·동작.

규칙: 공통은 위로, 특수는 아래로. 충돌하면 **더 구체적인(아래) 파일이 우선**한다. 같은 내용을 중복해서 적지 않는다.

---

## 폴더 구조
```
3d-simulator/
  CLAUDE.md                # ← 이 파일. 전체 공통
  shared/                  # 모든 모델이 공유하는 코드
    scene/                 #   Three.js 씬·카메라·조명·렌더 루프
    interaction/           #   회전/줌/분해 컨트롤, 레이캐스트(클릭 선택)
    ui/                    #   2단계 사이드바, 정보 패널, 상단 네비, 언어 전환
    styles/                #   CSS + 디자인 토큰(색·폰트 변수)
  pages/                   # 사이트 전체 공통 페이지 (소개 / 학습 구조 / 제작자 의도)
  locales/                 # 다국어 번역 파일 (ko / en / ja / zh ...)
  semiconductor/           # ── 카테고리 (분야)
    CLAUDE.md              #   카테고리 지침
    hbm/                   #     ── 모델
      CLAUDE.md            #       모델 지침
      model.ts             #       3D 형상
      data.ts              #       부품 설명 (다국어 키)
    gpu/  cpu/
  space/  automotive/  appliance/  aviation/  medical/  energy/  robotics/
```
**원칙**: 모델마다 다른 건 `model.ts`(형상)와 `data.ts`(설명)뿐이게 만든다.
회전·줌·분해·사이드바·정보패널 같은 공통 동작은 전부 `shared/`에 두고 모델들이 가져다 쓴다.
새 모델 추가 = `<카테고리>/<모델>/` 폴더 하나(형상 + 데이터 + 전용 CLAUDE.md)만 만들면 되도록.

---

## 카테고리 (현재 8개, 계속 확장)
반도체(semiconductor) · 우주(space) · 자동차(automotive) · 가전(appliance) ·
항공(aviation) · 의료기기(medical) · 에너지(energy) · 로보틱스(robotics)

추가 후보(원하면): 통신·네트워크 / 디스플레이 / 광학·카메라 / 건축·구조 / 해양.

---

## 사이트 구조 (UI)
- **상단 네비**: 홈 · 소개 · 학습 구조 · 제작자 의도 + 언어 전환(KO/EN/日/中).
- **좌측 2단계 사이드바**: 카테고리(1단계) → 클릭하면 그 아래로 모델 목록(2단계)이 아코디언으로 펼쳐짐.
- **본문 뷰**: 홈(소개형 히어로) / 카테고리 개요(모델 카드 그리드) / 모델 상세(3D 뷰어 + 설명) / 공통 페이지.
- **모델 상세**가 실제 3D 시뮬레이터가 뜨는 곳. 나머지는 그 입구.
- 모바일: 사이드바는 햄버거로 여닫는 드로어.

레퍼런스 시안: `homepage-mockup-v2.html` (이 구조와 디자인이 그대로 구현돼 있음).

---

## 기술 스택 (전체 공통)
- **빌드**: Vite
- **언어**: TypeScript (초반엔 과하게 엄격하게 굴지 말 것)
- **3D**: Three.js
- **스타일**: 순수 CSS + CSS 변수. UI 프레임워크는 쓰지 않는다.
- **다국어**: 가벼운 자체 i18n (locales의 키-값 사전 + 언어 전환). 텍스트는 코드에 하드코딩하지 말고 locales에 둔다.
- **배포 목표**: Vercel 또는 Netlify 무료 티어

새 라이브러리는 임의로 추가하지 말 것. 필요하면 먼저 이유를 설명하고 승인받는다.

---

## 디자인 방향 (전체 공통, 반드시 준수)
**컨셉**: 정밀 계측기(precision instrument) 느낌의 절제된 다크 테크 미감. Apple 제품 페이지나 Linear 같은 톤.

- **배경**: 거의 검정에 가까운 네이비 라디얼 그라데이션 (#04060c ~ #1b2438) + 아주 옅은 블루프린트 그리드.
- **포인트 색**: 블루(#6f9bff) + 구리(#c97b34) + 골드(#e6b53c). 채도 낮게, 고급스럽게.
- **UI**: 반투명 글래스 패널(backdrop-filter: blur), 얇은 흰색 테두리(낮은 불투명도).
- **타이포그래피**:
  - 제목·라벨·수치 → 모노스페이스: `ui-monospace, "SF Mono", Menlo, Consolas, monospace`
  - 본문 → 산세리프: `-apple-system, "Apple SD Gothic Neo", "Noto Sans KR", system-ui, sans-serif`
  - (선택) 더 개성 있는 웹폰트를 얹어 분위기를 강화해도 좋다.
- **모션**: 페이지 로드 시 staggered 등장, 카드 호버 리프트. 분해는 부드러운 가감속으로 "촤라락" 펼쳐지는 느낌(층마다 시차).
- 공통 색·폰트는 `shared/styles`의 CSS 변수로 관리. 모델별 포인트 색은 각 모델 CLAUDE.md에서 정한다.

### 절대 하지 말 것 (AI 사이트 티 방지)
- 보라색 그라데이션, 흰 배경 위 파스텔 / 둥근 이모지 남발 / 과한 그림자·네온
- Inter·Roboto·Arial 같은 흔한 폰트 / 정보 없이 형식만 채운 카드·배지 떡칠

---

## 작업 방식 (전체 공통, 중요)
1. **계획 먼저, 코드는 그다음.** 새 기능·큰 변경은 접근 방식을 먼저 제안하고 승인받는다.
2. **한 번에 한 기능.** 여러 기능을 한꺼번에 만들지 말 것. 하나가 동작하면 다음으로.
3. **동작 단위로 커밋.** 기능이 동작하면 의미 있는 메시지로 git 커밋.
4. **동작하는 코드를 함부로 갈아엎지 말 것.** 바꿀 거면 먼저 말한다.
5. **공통 vs 특수 구분 유지.** 여러 곳에 쓸 코드는 `shared/`, 특정 카테고리/모델 전용은 그 폴더에.
6. **반응형은 처음부터.** 데스크톱 + 모바일(세로). 터치(1손가락 회전, 2손가락 핀치) 지원.
7. **모든 사용자 노출 텍스트는 다국어 키로.** 하드코딩 금지.
8. 막히거나 모호하면 추측하지 말고 질문한다.

---

## 진행 순서 (전체)
1. **셋업** — Vite + TS, 폴더 구조, `shared/styles` 디자인 토큰, 빈 Three.js 씬.
2. **앱 셸 + 다국어** — 상단 네비, 2단계 사이드바, 공통 페이지(소개/학습 구조/제작자 의도), i18n 전환. (시안 `homepage-mockup-v2.html` 기준)
3. **공통 3D 토대** — `shared/`의 회전·줌·분해·정보패널을 더미 모델로 동작 확인.
4. **첫 모델 HBM** — `semiconductor/hbm/` 완성. 자세한 건 그 폴더 CLAUDE.md.
5. **두 번째 모델**로 `shared/` 재사용성 검증, 이후 카테고리·모델 확장.
6. **마감** — 성능, 자동 회전/초기화, 반응형 점검.

각 단계가 끝나면 다음으로 넘어가기 전에 확인받는다.

---

## 참고 자료 (이미 만들어 둔 것)
- `hbm-3d.html` — 동작하는 HBM 3D 분해도 프로토타입. 모델 디자인·인터랙션의 출발점.
- `homepage-mockup-v2.html` — 사이트 전체 디자인·구조 시안(2단계 사이드바, 공통 페이지, 다국어).
맨땅에서 시작하지 말고, 이 둘을 이 폴더 구조로 정리하는 방향이 가장 빠르다.
